<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oracle.hellong.PointChargeMapper">

	<select id="hsTotalListPointChargeCnt" parameterType="int" resultType="int">
		SELECT count(*)
		FROM point_charge
		WHERE m_number = ${m_number}
	</select>
	
	<select id="hsListPointCharge" parameterType="int" resultType="PointCharge">
		SELECT *
		FROM 
			(SELECT rownum rn, a.*
			 FROM
				(SELECT *
				 FROM point_charge 
				 WHERE m_number = ${m_number}
				 ORDER BY charge_date DESC
                 ) a
			)
	</select>

</mapper>