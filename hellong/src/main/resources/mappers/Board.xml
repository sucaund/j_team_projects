<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.hellong.BoardMapper">
	<select id="DYBoardCount" parameterType="java.lang.String" resultType="int">
	<!-- 이렇게 이름으로 구분(Mapper는 같이 쓰니까) -->
	</select>
	
	<select id="HSListBoard" parameterType="Board" resultType="Board">

	</select>
	
	<select id="JSInsertBoard" parameterType="Board">
	
	</select>

</mapper>