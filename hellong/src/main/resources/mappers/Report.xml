<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oracle.hellong.ReportMapper">

	<!-- 새 글을 쓰는 쿼리 -->
	<insert id="jjInReported" parameterType="int">
		INSERT INTO report ( b_number, report_id, m_number, common_bcd, common_mcd )
		VALUES ( #{b_number}, (SELECT NVL(MAX(report_id), 0) + 1 FROM report),
		         #{m_number}, #{common_bcd}, #{common_mcd}
		       )
		        

	</insert>

</mapper>